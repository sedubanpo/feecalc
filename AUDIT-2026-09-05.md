# 수강료 계산기 점검 — 2026-09-05

대상: `index.html`의 9개 모드, 계산·저장·복원·검색·내보내기 공통 코드 및 저장소 SQL.
기존 배포 기준: `5571e45`. 신규 프레임워크나 런타임 의존성 없이 수정했다.

## 수정 내역

| Before | After |
| --- | --- |
| 이력/납부 안내서가 공통 합계 ID를 제거하여 저장 목록에 0원 기록 | 두 모드에 합계 ID를 유지하고 저장 시작 시 합계와 대상 기록 ID를 함께 고정 |
| AI가 숫자 크기로 금액을 추측하고 다른 달·참고 문구를 오인 | Access 금액 열만 사용, 선택 월·실제 날짜 검증, 참고의 결제 문구 때문에 수업을 제외하지 않음 |
| AI의 마지막 결석/0원 이력 뒤에 이미 지난 날짜를 다시 예측 | 원본에서 관찰된 마지막 날짜 이후만 예측 |
| AI 이름·연월 표시가 이전 상태로 남음 | 렌더마다 이름·연월 갱신, 저장된 AI 데이터의 월 경계 유지 |
| 확정 0원 수업을 시간당 단가로 다시 과금 | 0원과 빈 금액 구분, 확정 0원 보존 |
| 24시간제 정오를 자정으로 읽고 빈 시간을 24시간으로 간주 | 오전/오후/24시간제 구분, 빈·잘못된 시간 및 같은 시작/종료 방어 |
| 잘못된 붙여넣기가 기존 이력/납부 내역을 제거 | 유효성 확인 후 교체, 분석 실패 시 기존 내역 유지 |
| 납부 참고 문구의 결제 단어를 납부로 오인, 음수 이월금을 납부로 분류 | 항목·반명·결제 구분으로 판별, 이월/초과금 우선 분류 |
| 1,000원 미만 할인이 무시되고 음수 할인이 과금을 늘림 | 작은 할인도 반영, 할인 범위를 0~해당 금액으로 제한 |
| 구형 저장본을 재분석하며 수동 수정 금액이 덮임 | 저장된 편집 금액 유지, 누락된 붙여넣기 원문은 이전 학생 값 대신 초기화 |
| 잘못된 JSON 행/숫자 데이터로 부분 복원 또는 HTML 삽입 위험 | 행·숫자·날짜 배열을 화면 초기화 전에 검증하고 숫자 정규화 |
| 검색/불러오기 응답이 늦게 도착하면 최신 요청을 덮음 | 요청 순서 검증, 검색 실패 시 기존 목록 보존 |
| 연속 저장·내보내기 클릭 중복 실행 | 실행 가드, 버튼 비활성/진행 상태, 종료 후 복구 |
| 저장 도중 새 계산·다음 달·다른 기록으로 대상 혼동 | 저장 완료 전 관련 전환 차단, 새 계산/다음 달은 이전 불러오기 응답 무효화 |
| 캘린더 HTML을 날짜마다 전체 재해석 | 셀 HTML을 모아 한 번에 반영 |
| 날짜를 마우스로만 조작 가능 | 날짜 버튼 역할·상태·이름, Enter/Space 조작과 포커스 복원 |
| 시간표에 2월 30/31일을 추가 과금 가능 | 현재 월에 실제 존재하는 추가 날짜만 계산 |
| 수업 시간 편집 중 요일고정 행 재정렬 | 모든 입력/선택 편집 중 재정렬 방지 |
| 음수 단가·횟수·시간이 음수 수강료 생성 | 수강료 계산 인자는 0 이상으로 방어, 이월금 입력은 별도 유지 |
| 문자 복사 실패가 무응답 | 실패 안내와 수동 복사 대안 제공 |
| PNG 대형 base64 문자열, 저장 중 날짜 변경으로 파일명 불일치 | Blob 다운로드, URL 해제, 파일명 날짜 고정 |
| PNG 작은 글자/날짜 잘림 | 캡처 전용 줄 높이·아래 여백·축소 방지·상세 문구 줄바꿈 |
| 큰 창에서 작은 창으로 바꾸면 도움말 위치가 화면 밖에 남음 | 창 크기 변경 시 기존 위치 계산을 다시 수행 |

## 검증

`tests/regression.cjs`: 실제 Chrome 152.0.7977.77 / macOS, Node 25.6.1 / Playwright. 14개 테스트 묶음 통과, 페이지 JavaScript 오류 없음.

- 9개 모드: 비영(非零) 금액 계산 및 저장 데이터 왕복 후 금액 일치.
- 확정 0원·시간·AI 원본 열·월 경계·불량 붙여넣기·수동 수정 복원.
- 검색 역순 응답·저장 중복·저장 스냅샷: RPC 모의 응답을 사용한 클라이언트 통합 검증.
- 당일취소 표시 전환 원복 및 원본/금액 보존, 선택형 기준일·키보드 조작.
- 186개 수업 렌더 및 실제 PNG 다운로드/크기 복구.
- 수치형 악성 입력 거부, 클립보드 거부 시 복구 안내.

실제 학생 기록은 테스트를 위해 생성·덮어쓰기·삭제하지 않았다. 프로덕션 DB의 영속성·권한 검증은 완료로 주장하지 않는다.

### 성능

동일한 186개 수업, 1440×1000 Chrome에서 캘린더 갱신 및 강제 레이아웃을 20회씩 5번 측정했다. 최종 로컬 검증의 중앙값은 기존 7.145ms, 수정 후 2.965ms(약 59% 감소). 캘린더 렌더 함수의 국소 측정이며 전체 앱·네트워크·모든 기기의 속도를 뜻하지 않는다. Lighthouse 점수는 측정하지 않았다.

### 실행

Playwright가 설치된 환경에서:

```sh
node tests/regression.cjs
```

필요하면 `NODE_PATH`로 Playwright 모듈 위치, `CHROME_PATH`로 Chrome 실행 파일, `EVIDENCE_ROOT`로 결과 폴더를 지정한다. 기본 Chrome 경로는 macOS 설치 경로다. 모든 Supabase 요청은 테스트 안에서 모의 처리한다.

## 미해결: 서버 접근 보호 (우선순위 높음)

공개 클라이언트에 공유 접속 코드가 포함되고 SQL RPC는 해당 코드로 권한을 판단한다. publishable key 자체가 비밀이어야 하는 것은 아니지만, 공개된 공통 코드를 학생 기록의 인증 수단으로 사용하는 것은 별도 문제다. 실제 악용이나 유출을 확인했다는 뜻은 아니다.

필요한 다음 조치: Supabase 프로젝트 관리자를 확인하고 직원 로그인·직원 허용 목록 기반 서버 권한을 마련한 뒤, 기존 공통 코드 폐기/회전과 익명 RPC 권한 정리를 함께 수행해야 한다. 프런트엔드 문자열만 제거해도 Git 기록/기존 배포본에 남으므로 해결되지 않는다. 사용자에게 관리자 접근 경로를 물었으나 현재 알 수 없다고 답변받았다. 기존 업무를 차단할 수 있는 인증/DB 변경은 하지 않았다. 이 항목은 해결 또는 위험 수용 완료로 처리하지 않는다.

## 나머지 한계

- AI 예측은 이름·요일·기존 단가에 기반한 규칙식 추정이다. 실제 미래 시간표와 변동 단가의 정확성을 보장하지 않는다.
- 자동 디자인 검사에는 기존 회색 텍스트/유색 배경 및 그림자 경고가 남아 있다. 이번 캡처 보정은 색상·전체 레이아웃을 재설계하지 않았다. WCAG 전체 준수나 사용성 연구 완료를 주장하지 않는다.
- 과거 서버 목록에 이미 저장된 0원 표시는 자동 일괄 수정하지 않았다. 해당 기록을 열고 확인 후 덮어쓰면 새 합계가 저장된다.
- 외부 CDN 장애 대응, 완전한 오프라인 운영, 동시 사용자 간 버전 충돌 방지는 별도 검증/설계가 필요하다.
