# 기능 QA 보고서

## 오류 재현과 원인

실제 저장본 `c308d78c-d8cb-4cd7-8373-fc776b74f70d`를 불러오면 납부 전용 요약 HTML이 `priceSummaryArea`를 교체한 상태에서 저장본 적용 코드가 `labelTotal.innerText`를 직접 갱신했다. 이때 `labelTotal`이 존재하지 않아 `Cannot set properties of null (setting 'innerText')`가 서버 상태 영역에 노출됐다.

## 수정 검증

| 항목 | 결과 |
| --- | --- |
| 저장본 적용 전 기본 요약 DOM 복원 | 통과 |
| 납부 탭 재진입 시 전용 안내서 재렌더 | 통과 |
| 실제 오류 저장본 상태 메시지 | 오류 없음 |
| 납부/차액 최종 합계 | `5,062,292원` |
| 납부 대과목 셀 | `수학 / 과학 / 영어 / 국어 / 사탐`만 표시 |
| 납부 같은 강사 그룹 | 9개 강사 행, 유형 그룹 `[2,3,2,2,1,3,2,2,2]` |
| 이력 실제 저장본 요약 | 37회 / 75시간, 결석예고 12건 제외, `4,451,875원` |
| 이력 상세 압축 | 기존 19개 반복 행을 9개 강사 행으로 정리 |
| 이력 캘린더 이벤트 | 37개 유지 |
| 표·캘린더 수평 오버플로 | 0 |
| 브라우저 실행 오류 | 0 |

## 정적 검사

- 인라인 JavaScript parse: 통과
- `git diff --check`: 통과
- `transition: all`: 없음
- `color-mix()`: 없음
- `.DS_Store`: 스테이징 대상에서 제외

## 범위

Supabase 스키마, 인증 구조, Access 출결 분류 기준은 변경하지 않았다. QA는 실제 저장본의 기능 수치와 익명 fixture의 시각 결과를 함께 확인했으며 별도 사용자 조사는 수행하지 않았다.
