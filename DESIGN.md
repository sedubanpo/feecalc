---
name: 에스에듀 반포관 수강료 계산기
description: 입력 수업과 예상 수업을 구분하는 한국어 업무 계산 화면
colors:
  primary: "#004094"
  bg: "#F8FAFC"
  surface: "#FFFFFF"
  fg: "#1F2937"
  muted: "#6B7280"
  border: "#E5E7EB"
  history: "#16A34A"
  caution: "#E11D48"
  free: "#0891B2"
typography:
  body:
    fontFamily: "Noto Sans KR, sans-serif"
    fontSize: "12px"
  label:
    fontFamily: "Noto Sans KR, sans-serif"
    fontSize: "12px"
    fontWeight: 700
rounded:
  control: "8px"
  badge: "4px"
spacing:
  step-1: "4px"
  step-2: "8px"
  step-3: "12px"
  step-4: "16px"
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.surface}"
    rounded: "{rounded.control}"
    padding: "8px 12px"
  button-secondary:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.fg}"
    rounded: "{rounded.control}"
    padding: "8px 12px"
  input:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.fg}"
    rounded: "{rounded.control}"
    padding: "8px"
---

# Design System: 에스에듀 반포관 수강료 계산기

## Overview

**Creative North Star: "고밀도 업무 문서의 신뢰감"**

고밀도 업무 문서의 신뢰감. 계산 결과와 상태 구분을 가장 먼저 읽히게 하며, 장식보다 정렬과 대비로 정보를 구분한다. 기존 에스에듀 블루와 기능별 상태색을 유지하고, 새 UI는 필요한 경우에만 추가한다.

**Key Characteristics:**

- 기존 청색·흰색·경계선 중심의 업무 화면을 확장한다.
- 입력과 예상은 명시적인 문구와 별도 목록으로 구분한다.
- 숫자는 정렬하고 조작 영역은 충분히 확보한다.

## Colors

| Token | Value | Role |
| --- | --- | --- |
| `--color-bg` | `#F8FAFC` | 작업 영역 배경 |
| `--color-surface` | `#FFFFFF` | 문서 및 입력 표면 |
| `--color-fg` | `#1F2937` | 본문 및 금액 |
| `--color-muted` | `#6B7280` | 보조 정보 |
| `--color-primary` | `#004094` | 에스에듀 기본 강조 |
| `--color-history` | `#16A34A` | 이력 확인 및 과금 확정 |
| `--color-caution` | `#E11D48` | 취소 및 주의 |
| `--color-free` | `#0891B2` | 무상 보충 |
| `--color-border` | `#E5E7EB` | 표와 입력 경계 |
| `--color-ring` | `#16A34A` | 이력 입력 포커스 |

기본 본문과 표면의 대비는 4.5:1 이상을 유지한다.

기존 기능별 색상은 화면 의미를 보존하기 위한 확장 팔레트로 관리한다.

| Group | Declared values | Role |
| --- | --- | --- |
| Neutral | `#FFFFFF`, `#FFF`, `#F8FAFC`, `#F8FBFF`, `#F4F8FF`, `#F3F4F6`, `#F1F5F9`, `#F1F1F1`, `#E5E7EB`, `#E2E8F0`, `#D1D5DB`, `#CBD5E1`, `#9CA3AF`, `#94A3B8`, `#64748B`, `#4B5563`, `#475569`, `#334155` | 배경, 경계, 보조 텍스트 |
| Blue / indigo | `#F0F7FF`, `#EAF2FF`, `#EDF0FF`, `#EFF6FF`, `#E0E7FF`, `#DBEAFE`, `#D9E8FF`, `#D2E0F6`, `#C7D2FE`, `#BFD7FB`, `#BFDBFE`, `#9AA9DC`, `#93C5FD`, `#8EA9C9`, `#7DA8DE`, `#5968AA`, `#496580`, `#486581`, `#3B82F6`, `#38BDF8`, `#3730A3`, `#355C9A`, `#35548F` | 수학, 선택, AI, 기록 상태 |
| Green / cyan | `#ECFDF5`, `#DCFCE7`, `#DAF4E4`, `#BFE9CF`, `#A7F3D0`, `#86EFAC`, `#7BBF98`, `#2F7A55`, `#2C6A4B`, `#166534`, `#047857` | 국어, 이력, 시간 상태 |
| Rose / orange / amber | `#FEF2F2`, `#FFF1F2`, `#FFF7ED`, `#FFF8DC`, `#FEE9D8`, `#FEE2E2`, `#FED7AA`, `#FECACA`, `#FECDD3`, `#FDE68A`, `#FDE3E3`, `#FAEFC9`, `#F8CCAB`, `#F6C7D1`, `#F3DFA8`, `#D592A3`, `#CFB075`, `#BE123C`, `#B7784B`, `#A65A66`, `#9F1239`, `#967243`, `#8E4F65`, `#84673B`, `#EF4444`, `#EA580C`, `#DC2626` | 취소, 시간표, 영어, 사회 상태 |
| Violet / specialty | `#F9E0EF`, `#F3BEDB`, `#ECE4FF`, `#E8E2FB`, `#D9C9F6`, `#D4C9F5`, `#B25D88`, `#9B8ACA`, `#7C3AED`, `#6D60A8`, `#6D58A8`, `#654D94` | 과학, 1:1, 2:1 상태 |

## Typography

현재 `index.html` 본문은 `"Noto Sans KR", sans-serif`를 사용한다. 기존 문서의 Arial 우선 스택은 현재 빌드와 달라 새 표면의 기준으로 삼지 않는다. 진행형 본문과 버튼은 12px, 제목과 버튼은 700 굵기이며 도움말 줄높이는 1.6이다. 기존 작은 보조 정보와 표의 10–11px 계층은 보존하되 진행형 설명을 그 크기로 축소하지 않는다. 숫자 요약과 금액은 `tabular-nums`를 사용한다.

## Layout

기본 단위는 4px이다. 현재 선언된 기본 변수는 `--space-1`부터 `--space-4`까지이며 값은 frontmatter의 단계와 같다. 문서 헤더는 20px와 24px 간격도 사용한다. 표 행은 12px 수직 여백, 입력 패널은 12px 안쪽 여백을 기준으로 한다. 기존 달력·뱃지의 정렬과 탭 안내 위치를 보존하는 광학 보정에는 `--space-optical-2: 2px`, `--space-optical-3: 3px`, `--space-optical-5: 5px`, `--space-optical-6: 6px`, `--space-optical-9: 9px`, `--space-optical-10: 10px`, `--space-optical-14: 14px`, `--space-tab-guide: 58px`를 사용한다.

600px 이하에서는 안내서 헤더가 줄바꿈되고 기본·시간표 본문이 세로로 배치된다. 저장 이미지의 고정 폭은 유지한다. 진행형 날짜 선택은 7열이며 각 열은 `minmax(0, 1fr)`로 폭을 나눈다. 예상 목록은 내용과 작업 버튼을 나란히 두고 긴 내용은 줄바꿈한다.

## Elevation & Depth

기본은 경계선과 표면 명도 차이로 구분한다. 기존 문서는 필요한 경우에만 얕은 그림자를 사용하는 방향을 유지한다. 진행형은 요약과 목록을 경계선으로 나누며 새로운 그림자나 중첩 카드를 추가하지 않는다.

## Shapes

입력과 버튼은 control 반경, 작은 상태 표지는 badge 반경을 사용한다. 기존 문서 표면과 표 경계를 유지하며 진행형 목록은 독립 카드 대신 수평 구분선으로 이어진다.

## Components

문서 패널은 흰색 표면, 8px 반경, `--color-border` 1px 경계를 사용한다. 상태 표지는 4px 반경, 6px 가로와 2px 세로 여백, 10px 굵은 글꼴을 사용한다. 버튼은 최소 40px 클릭 영역을 유지하고 hover, focus, active 상태를 제공한다. 과금 상태는 녹색, 취소 주의는 장미색, 무상 보충은 청록색으로 고정한다.

좁은 기록 패널의 제목과 작업 버튼은 서로 다른 행으로 분리한다. 수강료 단가 입력은 `회당`/`시간당` 단위를 같은 행에서 명시하고, 시간당 단가는 수업 시간 입력 또는 반명의 `-3h` 형식과 함께 계산한다. 금액 입력과 결과에는 `tabular-nums`를 적용한다.

납부/차액 안내서는 장식 카드나 왼쪽 색 띠 없이 하나의 선형 표로 구성한다. 열 순서는 `과목`, `강사·수업`, `산출 기준`, `금액`이며 같은 대과목은 첫 열의 병합 셀로 묶는다. 수업별 산출 기준은 가능한 경우 `회당 단가 × 횟수 · 시간/회`로 짧게 표시한다. Access 원본 대조 정보는 내부 편집 영역에만 두고 학부모 안내서에는 노출하지 않는다. 편집 표와 안내서 금액에는 `tabular-nums`를 유지하며 삭제 버튼은 40px 클릭 영역을 사용한다.

납부/차액 안내서에서 같은 대과목·강사의 여러 수업은 한 행으로 합치고, 유형별 산출 기준은 행 안에서 최대 2열로 병렬 배치한다. 대과목 셀에는 과목명만 표시하며 항목 수나 총 횟수 같은 보조 집계는 넣지 않는다.

이력확인 안내서는 월간 캘린더를 최상단에 유지한다. 각 날짜의 일정은 `과목 · 수업 유형`과 `시간`의 2단 정보로 표시하고, 강사명과 금액은 아래 요약 표에서 확인한다. 상세 표는 `과목`, `강사`, `수업 내역`, `금액` 순서로 구성하며 같은 대과목은 병합하고 같은 강사의 여러 유형은 한 행 안에서 병렬 요약한다. 과목 색은 작은 유형 표지에만 제한적으로 사용하고 행 전체를 장식 카드로 만들지 않는다.

기본 수강료 안내서의 캘린더는 전체 폭의 48% 내외를 확보한다. 일정 표시는 `과목명 + 작은 1:1 표지` / `시간` 순서의 2단 구조로 제한해 수업이 많은 주의 세로 팽창을 줄인다. 이미지 저장은 1120px 기준 폭을 사용하고, 한글 하단이 잘리지 않도록 캡처 전용 줄높이와 아래쪽 여백을 보장한다.

캡처 전용 작은 텍스트는 `--line-height-capture: 1.6`과 기존 `--space-optical-2` 아래 여백을 사용한다. 날짜는 축소되지 않도록 고정하고, 표의 작은 상세 문구는 저장 시 자동 줄바꿈하여 글자를 자르지 않는다. 이는 이미지 변환기의 글꼴 기준선 차이를 흡수하기 위한 규칙이며 편집 화면의 밀도는 바꾸지 않는다.

선택형의 날짜 조정은 `과목 날짜`와 `전체 제외` 두 동작을 명확히 분리한다. `선택일까지 계산`은 기준일 다음 날부터 월말까지를 전체 제외일로 표시하고, 제외된 날짜는 과목별 선택 횟수·수강료·안내서 캘린더에서 동시에 빠져야 한다. 전체 제외일은 날짜 목록과 건수를 한 줄로 요약하며 언제든 일괄 해제할 수 있어야 한다.

이월·초과금은 단일 숫자가 아닌 `내용 + 부호 있는 금액` 행의 반복 목록으로 입력한다. 이월금은 음수, 초과금은 양수로 입력하며 최소 40px 높이의 입력·삭제 조작부를 사용한다. 안내서와 문자는 각 항목명을 그대로 표시하고, 둘 이상이면 조정 합계를 추가한다. 공통 조정 목록은 요일고정·선택형·변동형·안내형·첫등록·시간표·이력확인·AI예측에서 동일하게 사용하고, 납부/차액은 기존 편집 표의 `조정` 행이 같은 역할을 담당한다.

이력확인·AI예측·납부/차액의 `당일취소를 출석으로` 옵션은 각 기능의 기존 강조색을 옅은 배경으로 재사용한 최소 40px 높이의 체크 행으로 제공한다. 체크 상태는 저장 기록에 포함한다. 이력확인은 금액을 유지한 채 집계·캘린더·내역서의 상태를 출석으로 통합하고, AI예측은 당일취소 건을 출석 패턴으로 포함하며, 납부/차액은 편집 원본을 보존한 채 안내서 표기만 출석으로 정규화한다.

### 상태 전환

현재 버튼은 160ms `cubic-bezier(0.2, 0, 0, 1)`로 배경·경계·텍스트·그림자·transform 상태를 전환하고, 누르는 동안 scale(0.96)을 사용한다. `prefers-reduced-motion`에서는 전환을 제거한다. 진행형은 별도 모션을 추가하지 않는다.

### 직원 로그인 (2026-09-06)

저장 기록 패널의 자동 연결 안내 위치에 기존 계정 로그인 폼을 둔다. 기존 slate 표면·경계, sky 주 버튼, 4px 간격 체계와 최소 40px 조작 영역을 재사용한다. 이메일 또는 휴대전화 번호와 비밀번호에는 명시적 레이블·자동 완성 의미를 제공하며 상태는 `role="status"`로 알린다. 별도 모달이나 신규 색상·모션은 추가하지 않는다. 계산·이미지 작업은 로그인 여부와 관계없이 유지한다. 로그아웃 버튼은 입력 초기화 결과를 명시하고 확인 후 실행한다.

### 학생 연결·단가·문서 헤더 (2026-09-07)

기존 청색/흰색 업무 화면을 유지한다. 학생 입력 아래에 매칭 상태와 학교·학년별 후보 버튼을 놓고, 미매칭은 임시 계산임을 알린다. 내부 메모는 데스크 원본을 읽기 전용으로 표시하고 안내 이미지에는 넣지 않는다. 기본 계산 JSON 저장/불러오기 조작부는 삭제한다.

단가는 유형 이름과 금액/단위를 구분선으로 정렬한 목록으로 표시한다. 개별정규, 1:1, 2:1, 3:1, 4:1, 특강, 컨설팅, 수행평가, 기타 순서를 사용한다. 편집은 펼침 영역, 각 과목의 적용은 유형과 단가 선택 상자로 제공한다. 40px 조작 높이, 8px 반경, 8/12px 간격과 기존 semantic 색상만 사용한다. 등록되지 않은 유형은 빈 상태로 명시하며 임의 금액을 만들지 않는다.

문서 헤더는 좌측 로고·학원명·문서명, 우측 학생명·년월을 한 줄에 배치한다. 좁은 600px 이하 화면에서는 헤더와 본문이 줄바꿈되며 저장 이미지의 고정 폭은 유지한다. 새 헤더 간격은 24px, 아래 여백/간격은 20px, 구분선은 기본 청색 2px이다.

조정 행은 이월금/초과금/기타 선택 버튼, 내용과 금액, 삭제 순서이다. 선택 상태를 색상과 aria-pressed로 함께 표현한다. 이월금은 음수, 초과금은 양수로 정규화하며 기존 항목과 기타는 사용자 입력을 보존한다. 전월 기본명은 유형을 선택할 때 적용한다.

### 진행형 (2026-09-16)

진행형 안내서는 과목별 소제목과 강사별 한 행으로 압축한다. 시간·유형·횟수와 그중 예상 횟수를 같은 행 안에 표시하고 반복적인 `입력` 접두어는 생략한다. 결석예고는 별도 작은 문구와 0원으로 남긴다. 캘린더는 `minmax(0,1fr)` 7열을 사용하고 모든 날짜·일정이 줄바꿈되어 보이도록 한다. 안내서 가용 폭 680px 이상에서는 내역 52%·달력 46%로 나란히 배치하며, 그 아래에서는 세로 배치하여 달력 전체 폭을 확보한다. 8px 행 여백과 12px 본문을 사용한다. 결석예고 일정만 neutral 배경·muted 글자와 `결석 · 0원`으로 어둡게 표시하며 예상 수업의 점선 표지와 구별한다. 원본 결석과 다음 주 정상 예상은 별개이며 원본을 변경하지 않는다.

기존 계산 방식 탭과 기본 설정 아래에 통합한다. 인트라넷 조회를 기본 청색 버튼으로 제공하고, 처리 상태는 `role="status"`로 알린다. 남은 요일 자동 계산과 날짜 직접 추가는 네이티브 라디오 두 개로 선택한다. 입력 수업은 접힌 상세 영역에 두고 예상 수업만 제외·복구·날짜 추가 조작을 제공한다. 날짜 버튼은 `aria-pressed`로 선택 상태를 전달한다.

**The Input/Forecast Rule.** 입력분과 예상분은 요약·목록·안내서·문자에서 이름으로 구분한다. 색상만으로 구분하지 않는다. 확인되지 않은 금액은 `확인 필요`로 표시하고, 확인 전 저장·내보내기를 완료 가능한 상태로 보이지 않게 한다.

입력·선택·날짜·제외 버튼은 최소 40px 높이를 유지한다. 키보드 포커스는 기본 청색 2px 외곽선과 2px 간격을 사용한다. 비활성 버튼은 0.5 불투명도와 금지 커서를 사용한다. 입력/예상 요약은 위아래 경계선과 1.8 줄높이로 읽기 순서를 확보한다. 안내서 금액은 우측 정렬하고 줄바꿈하지 않으며, 합계 영역 자체는 좁은 화면에서 줄바꿈을 허용한다.

**The Context Rule.** 학생·월이 바뀌면 이전 학생의 데이터를 숨기고 재조회를 안내한다. 저장본은 저장 시점 스냅샷임을 표시한다. 원본 오류나 단가 확인이 필요한 경우 수정 위치와 다음 행동을 문구로 알려준다. 내부 메모는 안내서에 넣지 않는다.

문자와 안내서에는 이월·초과금 등 각 조정 항목의 내용과 부호 있는 금액을 유지한다. 합계만 남겨 조정 근거를 없애지 않는다. 기존 9개 모드의 표현과 조작은 이 확장 때문에 바꾸지 않는다.

## Do's and Don'ts

- Do 기존 청색·흰색 표면, 경계선, 한국어 레이블과 입력/예상 구분을 유지한다.
- Do 40px 조작 높이, 명확한 포커스와 숫자 정렬을 유지한다.
- Do 안내서와 문자에 조정 항목의 내용·금액을 보존한다.
- Don't 미확인 금액을 확정된 0원처럼 표시한다.
- Don't 진행형의 읽기 전용 입력 수업에 편집 가능한 조작을 붙인다.
- Don't 새 장식 카드나 새 색상으로 기존 업무 화면의 계층을 대체한다.
